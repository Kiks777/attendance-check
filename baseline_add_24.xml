
package com.termproject.ac;

import android.content.ContentValues;
import android.content.Intent;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.os.Bundle;
import android.view.View;
import android.widget.TextView;

import androidx.appcompat.app.AlertDialog;
import androidx.appcompat.app.AppCompatActivity;
import androidx.recyclerview.widget.LinearLayoutManager;
import androidx.recyclerview.widget.RecyclerView;

import com.google.android.material.floatingactionbutton.FloatingActionButton;
import com.termproject.ac.database.DatabaseHelper;

import java.util.ArrayList;
import java.util.List;

public class EventsListActivity extends AppCompatActivity {

    private RecyclerView recyclerView;
    private EventsListAdapter adapter;
    private List<String> eventList;
    private FloatingActionButton add_button;
    private TextView no_event_added_message;
    private DatabaseHelper dbHelper;
    private SQLiteDatabase db;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.events_check);

        no_event_added_message = findViewById(R.id.no_event_added_message);

        dbHelper = new DatabaseHelper(this);
        db = dbHelper.getWritableDatabase();

        // Initialize RecyclerView
        recyclerView = findViewById(R.id.recyclerView);
        recyclerView.setLayoutManager(new LinearLayoutManager(this));

        // Initialize the list of event names
        eventList = new ArrayList<>();
        loadEventsFromDatabase();

        // Initialize the adapter and set it to the RecyclerView
        adapter = new EventsListAdapter(eventList, this);
        recyclerView.setAdapter(adapter);

        // Setup FloatingActionButton
        add_button = findViewById(R.id.add_button);
        add_button.setOnClickListener(v -> {
            Intent intent = new Intent(EventsListActivity.this, AddNewEvent.class);
            startActivityForResult(intent, 1); // Start activity with request code 1
        });

        checkEventListStatus();
    }

    private void loadEventsFromDatabase() {
        Cursor cursor = db.query("Events", null, null, null, null, null, null);
        while (cursor.moveToNext()) {
            String eventName = cursor.getString(cursor.getColumnIndexOrThrow("event_name"));
            eventList.add(eventName);
        }
        cursor.close();
    }

    private void insertEventIntoDatabase(String eventName) {
        ContentValues values = new ContentValues();
        values.put("event_name", eventName);
        db.insert("Events", null, values);
    }

    @Override
    protected void onActivityResult(int requestCode, int resultCode, Intent data) {
        super.onActivityResult(requestCode, resultCode, data);

        // Check the request code and result code
        if (requestCode == 1 && resultCode == RESULT_OK && data != null) {
            // Get the new event name from the intent extras
            String newEventName = data.getStringExtra("newEventName");

            // Check if the new event name is null or empty
            if (newEventName != null && !newEventName.trim().isEmpty()) {
                // Add the new event name to the eventList
                eventList.add(newEventName);

                // Insert the new event into the database
                insertEventIntoDatabase(newEventName);

                // Notify the adapter of the changes
                adapter.notifyDataSetChanged();
                checkEventListStatus();
            } else {
                // Display an alert dialog if the event name is empty or null
                AlertDialog.Builder builder = new AlertDialog.Builder(this);
                builder.setTitle("Invalid Input")
                        .setMessage("Event name cannot be empty.")
                        .setPositiveButton("OK", (dialog, which) -> {
                            // Dismiss the dialog when the OK button is clicked
                            dialog.dismiss();
                        });
                // Create and show the alert dialog
                AlertDialog alertDialog = builder.create();
                alertDialog.show();
            }
        }
    }

    public void checkEventListStatus() {
        // Check if eventList is either null or empty
        if (eventList == null || eventList.isEmpty()) {
            // Show the message if the list is null or empty
            no_event_added_message.setVisibility(View.VISIBLE);
        } else {
            // Hide the message if the list is not null and not empty
            no_event_added_message.setVisibility(View.GONE);
        }
    }
}
